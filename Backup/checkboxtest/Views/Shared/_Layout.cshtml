<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title</title>
    <link href="@Url.Content("~/Content/Site.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Scripts/jquery-1.7.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/modernizr-2.5.3.js")" type="text/javascript"></script>
</head>
<body>
    <div class="page">
        <header>
            <div id="title">
                <h1>Enquete systeem</h1>
            </div>
            <div id="logindisplay">
                @Html.Partial("_LogOnPartial")
            </div>
            <nav>
                
                <ul id="menu">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("Enqueteresultaten bekijken", "Index", "Resultaat")</li>
                    <li>@Html.ActionLink("Enquetes beheren", "Index", "Enquete")</li>
                    <li>@Html.ActionLink("Enquetegroepen beheren", "Index", "Groep")</li>
                    <li>@Html.ActionLink("Groepsvragen beheren", "Index", "Question")</li>
                    <li>@Html.ActionLink("Gebruikers beheren", "Index", "Gebruiker")</li>
                    
                </ul>
               
                @*
                @if (Session["mijnMenu"] == null){
                    Session["mijnMenu"] = Html.Partial("~/Views/Shared/_navigatieMenu.cshtml");
                } 
                @Session["mijnMenu"]
                *@
            </nav>
        </header>
        <section id="main">
            @RenderBody()
        </section>
        <footer>
        </footer>
    </div>
</body>
</html>
